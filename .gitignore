# =========================================================================
# A comprehensive .gitignore file for modern web projects.
# =========================================================================

# Dependencies & Package Managers
# These can be reinstalled from package.json, so they don't belong in the repo.
/node_modules/
/bower_components/
/vendor/
npm-debug.log*
yarn-error.log
pnpm-debug.log

# Build output & Compiled files
# Files generated by your build process.
/dist/
/build/
/.out/
/.next/
/.cache/

# Environment variables
# Never commit secrets! Keep a .env.example file in the repo instead.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs and temporary files
# Log files can be very large and are specific to one environment.
*.log
logs/
*.log.*
.tmp/

# Operating System files
# Annoying files created by macOS, Windows, and Linux.
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini
*~

# IDE & Editor configuration
# Your personal editor settings don't need to be shared with the team.
# (You can selectively un-ignore files like .vscode/settings.json if the team agrees on them)
/.vscode/
/.idea/
*.sublime-project
*.sublime-workspace
*.suo
*.user
*.sln.docstates
*.swp

# Testing & Coverage
# Test reports and coverage results are generated on each run.
/coverage/
/.nyc_output/

# Miscellaneous
# Any other local configuration or temporary files.
.pnp.*